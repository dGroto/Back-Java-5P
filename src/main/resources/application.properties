spring.application.name=mensal
spring.datasource.url=jdbc:mysql://database.dua.dev.br:3306/mensal
spring.datasource.username=debora
spring.datasource.password=debora

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

#KEYCLOAK CONFIGURATION
keycloak.server-url=${KEYCLOAK_SERVER_URL:http://192.168.3.2:5001}
keycloak.realm=${KEYCLOAK_REALM:bia-mensal}

spring.security.oauth2.resourceserver.jwt.issuer-uri=${keycloak.server-url}/realms/${keycloak.realm}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${keycloak.server-url}/realms/${keycloak.realm}/protocol/openid-connect/certs

keycloak.admin.realm=master
keycloak.admin.client-id=debora
keycloak.admin.secret=yZmrlIJYBht8weP0Pqq3JgyDwoGfwEy7
app.role.prefix=sys-alpha


# CORS Configuration
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*